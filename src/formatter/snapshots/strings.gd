# --- IN ---
func f():
	var strings = [", ",   "",   "  ",   ", , "]
	print("name: %s" % name)
	print("%s / %s" % [a, b])
	print("%s/%s" % [a, b])
# --- OUT ---
func f():
	var strings = [", ", "", "  ", ", , "]
	print("name: %s" % name)
	print("%s / %s" % [a, b])
	print("%s/%s" % [a, b])
