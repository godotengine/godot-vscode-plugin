// Re-export the main classes for the property inspector
export { PropertyInspector } from "./property_inspector";
export { NodePropertiesWebviewProvider } from "./webview_provider";

// Re-export types that might be needed externally
export type { PropertyData, PropertyEditorInfo } from "./types";
